{
  "name": "swarm-messaging",
  "version": "1.0.0",
  "description": "MCP server for Agent Swarm messaging system - send messages to agents via PyAutoGUI coordinates",
  "protocol": "mcp",
  "command": "python",
  "args": ["mcp_servers/messaging_server.py"],
  "tools": [
    {
      "name": "send_agent_message",
      "description": "Send a message to a specific agent via PyAutoGUI at their chat input coordinates",
      "parameters": {
        "agent_id": {
          "type": "string",
          "description": "Target agent ID (Agent-1 through Agent-8)",
          "required": true
        },
        "message": {
          "type": "string",
          "description": "Message content to send to the agent",
          "required": true
        },
        "priority": {
          "type": "string",
          "enum": ["regular", "urgent"],
          "default": "regular",
          "description": "Message priority level"
        }
      }
    },
    {
      "name": "broadcast_message",
      "description": "Broadcast a message to all agents in the swarm",
      "parameters": {
        "message": {
          "type": "string",
          "description": "Message content to broadcast",
          "required": true
        },
        "priority": {
          "type": "string",
          "enum": ["regular", "urgent"],
          "default": "regular",
          "description": "Message priority level"
        }
      }
    },
    {
      "name": "get_agent_coordinates",
      "description": "Get coordinates and status information for all agents in the swarm",
      "parameters": {}
    }
  ]
}




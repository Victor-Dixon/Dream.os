apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: dadudekc-com-vs
  namespace: production
spec:
  gateways:
  - migrated-sites-gateway
  hosts:
  - dadudekc.com
  - www.dadudekc.com
  http:
  - match:
    - uri:
        prefix: /
    retries:
      attempts: 3
      perTryTimeout: 10s
    route:
    - destination:
        host: dadudekc-com-service
        port:
          number: 80
    timeout: 30s

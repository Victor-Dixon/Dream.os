apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: houstonsipqueen-com-vs
  namespace: production
spec:
  gateways:
  - migrated-sites-gateway
  hosts:
  - houstonsipqueen.com
  - www.houstonsipqueen.com
  http:
  - match:
    - uri:
        prefix: /
    retries:
      attempts: 3
      perTryTimeout: 10s
    route:
    - destination:
        host: houstonsipqueen-com-service
        port:
          number: 80
    timeout: 30s

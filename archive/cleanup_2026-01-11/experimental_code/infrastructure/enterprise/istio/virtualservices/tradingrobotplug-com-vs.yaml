apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: tradingrobotplug-com-vs
  namespace: production
spec:
  gateways:
  - migrated-sites-gateway
  hosts:
  - tradingrobotplug.com
  - www.tradingrobotplug.com
  http:
  - match:
    - uri:
        prefix: /
    retries:
      attempts: 3
      perTryTimeout: 10s
    route:
    - destination:
        host: tradingrobotplug-com-service
        port:
          number: 80
    timeout: 30s

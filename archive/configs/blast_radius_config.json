{
  "limits": {
    "cost": {
      "max_per_action": 50.0,
      "max_per_hour": 250.0,
      "max_per_day": 1000.0,
      "warning_threshold": 0.8,
      "enforcement_mode": "strict"
    },
    "files": {
      "max_per_action": 5,
      "max_per_hour": 25,
      "max_per_day": 100,
      "warning_threshold": 0.8,
      "enforcement_mode": "strict"
    },
    "api_calls": {
      "max_per_action": 500,
      "max_per_hour": 2500,
      "max_per_day": 10000,
      "warning_threshold": 0.8,
      "enforcement_mode": "strict"
    },
    "db_writes": {
      "max_per_action": 50,
      "max_per_hour": 250,
      "max_per_day": 1000,
      "warning_threshold": 0.8,
      "enforcement_mode": "strict"
    },
    "compute": {
      "max_per_action": 10,
      "max_per_hour": 50,
      "max_per_day": 200,
      "warning_threshold": 0.8,
      "enforcement_mode": "strict"
    }
  },
  "metadata": {
    "description": "Production blast radius limits for autonomous operations",
    "version": "1.0",
    "last_updated": "2026-01-08",
    "author": "Agent-7"
  }
}
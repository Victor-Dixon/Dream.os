# Agent Refactor Project Archive

This directory intentionally contains no git submodule. The previous gitlink entry
has been removed to prevent CI cleanup failures caused by a broken submodule
configuration.

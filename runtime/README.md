<!-- SSOT Domain: runtime -->
# Runtime Storage

This folder is the single source of truth for swarm-console runtime artifacts:

- `runs/`: run records, summaries, and patch exports
- `agents/`: active agent roster
- `logs/`: streaming logs (placeholder)
- `ssot/`: active repo and session state
